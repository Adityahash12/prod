// Ensure the component is exported correctly
const Dashboard = () => {
    // Component logic
    return (
        <div>
            {/* Component JSX */}
        </div>
    );
};

export default Dashboard; // Ensure default export